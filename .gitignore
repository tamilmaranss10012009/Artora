# ---------- Artora runtime / secrets ----------

# Local environment secrets (see .env.example)
.env
.env.*

# Server runtime state (not version-controlled)
server/logs
server/data
server/uploads

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*.swo

